# Copy this file to .env and fill in real values before running docker compose up.

# Long random secret used to sign JWTs. Generate one with e.g.:
#   openssl rand -base64 48
JWT_KEY=

# Optional overrides (sensible defaults are used if omitted)
# POSTGRES_PASSWORD=postgres
# ASPNETCORE_ENVIRONMENT=Development

# Domínio público do frontend (usado pela API para liberar CORS) e da API (embutido
# no bundle do frontend em build-time, via VITE_API_URL). Ajuste se os subdomínios
# reais em *.gtlabs.com.br forem diferentes destes.
# FRONTEND_ORIGIN=https://app.gtlabs.com.br
# API_ORIGIN=https://api.gtlabs.com.br
